# requirements_community.txt
# ============================================================================
# Dependances pour AltiusOne Community Edition (AGPL-3.0)
#
# Differences avec requirements.txt (cloud/payant):
#   - PAS de altiusone-ai SDK (AI via MCP ou API compatible OpenAI)
#   - PAS de django-storages[google] (pas de GCS — MinIO/S3 ou filesystem)
#   - Tout le reste est identique
#
# Usage:
#   pip install -r requirements_community.txt
# ============================================================================

# Django Core
Django>=6.0,<7.0
asgiref>=3.9.0

# Database
psycopg[binary]>=3.2.0

# REST API
djangorestframework>=3.16.0
djangorestframework-gis>=1.2.0
djangorestframework_simplejwt>=5.5.0
django-cors-headers>=4.9.0
django-filter>=25.0
drf-spectacular>=0.28.0

# OAuth2 / OIDC Provider
django-oauth-toolkit>=3.0.0

# WSGI Server (Production)
gunicorn>=23.0.0

# Celery & Redis
celery>=5.4.0
redis>=5.0.0
kombu>=5.5.0

# Internationalization
django-modeltranslation>=0.19.0
django-countries>=7.6.0

# PDF & Excel
reportlab>=4.4.0
openpyxl>=3.1.0
xlsxwriter>=3.2.0

# Charts
plotly>=5.18.0
kaleido==0.2.1

# Import/Export
django-import-export>=4.3.14
tablib[all]>=3.5.0

# Forms & Templates
django-widget-tweaks>=1.5.0

# Images
pillow>=11.0.0

# QR Code & Swiss billing
qrcode>=8.0
qrbill>=1.2.0
svglib>=1.6.0

# Utilities
python-dateutil>=2.9.0
pytz>=2025.0
whitenoise>=6.11.0
Faker>=37.0.0
faker-file[common,pdf,docx,xlsx,images]>=0.18.0

# Security
cryptography>=45.0.0
bcrypt>=4.3.0
django-fernet-fields>=0.6

# Development & Testing
pytest>=8.4.0
pytest-cov>=7.0.0
coverage>=7.10.0

# S3 Storage (MinIO)
django-storages>=1.14
boto3>=1.34

# Vector Search
pgvector>=0.3.0
numpy>=1.26.0

# AI: PAS de altiusone-ai SDK
# Utiliser AI_API_URL + AI_API_KEY pour pointer vers Ollama, vLLM, LiteLLM, etc.
# Le serveur MCP (mcp/) permet de connecter n'importe quel LLM.
