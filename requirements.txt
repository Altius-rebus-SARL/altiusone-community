# Django Core
Django>=6.0,<7.0
asgiref>=3.9.0

# Database
psycopg[binary]>=3.2.0

# REST API
djangorestframework>=3.16.0
djangorestframework-gis>=1.2.0
djangorestframework_simplejwt>=5.5.0
django-cors-headers>=4.9.0
django-filter>=25.0
drf-spectacular>=0.28.0

# OAuth2 / OIDC Provider (pour Docs)
django-oauth-toolkit>=3.0.0

# WSGI Server (Production)
gunicorn>=23.0.0

# Celery & Redis
celery>=5.4.0
redis>=5.0.0
kombu>=5.5.0

# Internationalization
django-modeltranslation>=0.19.0
django-countries>=7.6.0

# PDF & Excel
reportlab>=4.4.0
openpyxl>=3.1.0
xlsxwriter>=3.2.0

# Charts (Plotly pour génération d'images de graphiques)
# Note: kaleido 0.2.1 est la dernière version stable avec navigateur intégré
# Les versions >=1.0 nécessitent Chrome installé séparément et ont des problèmes de timeout
plotly>=5.18.0
kaleido==0.2.1

# Import/Export
django-import-export>=4.3.14
tablib[all]>=3.5.0

# Forms & Templates
django-widget-tweaks>=1.5.0

# Images & OCR
pillow>=11.0.0
pytesseract>=0.3.10
pypdf>=5.1.0
pdf2image>=1.17.0

# QR Code & Swiss billing
qrcode>=8.0
qrbill>=1.2.0
svglib>=1.6.0

# Utilities
python-dateutil>=2.9.0
pytz>=2025.0
whitenoise>=6.11.0
Faker>=37.0.0
faker-file[common,pdf,docx,xlsx,images]>=0.18.0

# Security
cryptography>=45.0.0
bcrypt>=4.3.0
django-fernet-fields>=0.6
pyotp>=2.9.0

# Development & Testing
pytest>=8.4.0
pytest-cov>=7.0.0
coverage>=7.10.0

# Cloud Storage
django-storages[google]>=1.14
boto3>=1.34

# Vector Search & AI
pgvector>=0.3.0
numpy>=1.26.0

# Local AI - Embeddings (sentence-transformers)
# Note: torch CPU est installé séparément dans le Dockerfile (--index-url whl/cpu)
# Versions pinees pour compatibilité huggingface_hub/transformers
sentence-transformers>=3.0.0,<4.0.0
huggingface-hub>=0.25.0,<0.31.0
transformers>=4.46.0,<4.49.0

# Push Notifications
# - Android: FCM (firebase-admin requis par django-push-notifications pour FCM v1)
# - iOS: APNs direct (pas de Firebase)
# - Web: VAPID/Web Push (pas de Firebase)
django-push-notifications>=3.1.0
firebase-admin>=6.0.0
apns2>=0.7.0
pywebpush>=2.0.0

# AltiusOne AI SDK (OCR legacy — sera supprimé)
# altiusone-ai>=1.3.0  # DEPRECATED: remplacé par sentence-transformers + Ollama local
